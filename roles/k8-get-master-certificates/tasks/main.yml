- name: Get k8 certificates from master 0
  fetch:
    src: "/etc/kubernetes/pki/{{ item }}"
    dest: ../roles/k8-send-master-certificates/files/
    flat: yes
  with_items:
     - ca.crt
     - ca.key
     - sa.key
     - sa.pub