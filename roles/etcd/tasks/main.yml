- name: Start etcd in a pod running on the host
  script: setup_etcd_pod.sh
  become: yes
  environment:
    MASTER_URL: "{{ inventory_hostname }}"
    INVENTORY: "{{ inventory_hostnames }}"
    ARCHITECTURE: "{{ architecture }}"